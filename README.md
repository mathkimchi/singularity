# Sonamu

> Organize everything.

Sonamu let's you organize all your apps and their elements into one large hierarchy.

## The Problem

In the standard modern desktop UI, your workspace is organized into virtual desktops and then windows and then whatever organizer each app uses. This window-based organizer comes with a few limitations:

For each tab within the app (eg: browser tab, editor tab, or the entire app for simple apps), organization is limited to within the app window, preventing you from forming connections across tabs and requiring you to use a new UI to traverse each app. This can be solved via a unified organization protocol.

Window-level organization replicates physical space, but it can quickly get cluttered just like a small desk with too many things. Most apps just store tabs in a list, which is hardly any organization. Neither system was intended to support connections between items.
In contrast, a tree-based hierarchicy is born from each node's relationship to other nodes, which reflects how people naturally think and organize ideas.
It also enables you to traverse at different depths, which enables speed and precision.

## Who is Sonamu for?

I am building Sonamu for personal use, but conceptually, I believe a standardized hierarchical organizer is better than the current status quo of the window-based paradigm for anyone who has many projects and apps open at a time.

## Features

Brag Sheet: (technical features I'm proud of & things that taught me a lot)
- Entirely rendered on GPU with WGSL shaders
 - Grid of monospace chars rendered with multi-channel SDF for sharper corners (atlas generated by [`msdfgen` library](https://crates.io/crates/msdfgen), I wrote the render code)
 - Primitive shapes done with SDFs, image shader just samples a texture
 - Learned how to do wgsl and run it in wgpu, including the different buffers and how to set them up
 - Render logic is mostly done pixel-wise in the vertex shader
 - Use `wgpu` to run the shaders
- Made a minimal Wayland Compositor to embed Wayland apps as Sonamu apps
 - Used [Smithay library](https://github.com/Smithay/smithay/) which is also used by System76's Cosmic desktop and Niri
- Editor that uses the [`ropey` library](https://github.com/cessen/ropey) for efficient modification
 - This is what I'm currently working on [#37](https://github.com/mathkimchi/singularity/issues/37)
- My own implementation with a CLI with minimal compatibility with `sh`
- Applet-running framework
 - Built on custom GPU ui framework
 - Allows for embedded applets and a recursively structured hierarchy
 - Efficiently supports passive and active applets by let applets run event handlers in their own thread
 - Synchronization code
 - Previously used dynamic libraries to allow running new applets without restarting Sonamu, but removed it temporarily in favor of easier development
 - Previously also had an extendable packet system where applets and runners could use extra packet types if they both supported it
 - Also also previously, I could save the current workspace, but I also temporarily disabled it bc it slowed down making new applets
- Come up with a World Tree data structure to store trees with multiple levels of hierarchy
 - Main point of Sonamu
- Custom declarative (`macro_rules!`) and procedural macros (where functions modify Rust code at compile time)
 - Though most of them were replaced when I restructured to a simpler architecture
 - Build scripts as well

For features in progress or suggestions,
visit [the issues page](https://github.com/mathkimchi/singularity/issues).

<!-- ## What is singularity?

Singularity is an organization tool for projects.
It aims to be the only app needed for all things productive on a computer.
To do this, it needs to be highly flexible and customizable, with the help of subapps.
Though subapps should be given freedom, singularity aims to outline best practices for subapps from the perspective of users. -->
