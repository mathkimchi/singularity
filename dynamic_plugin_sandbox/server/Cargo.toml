[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
# currently a temp measure for testing
dynamic-plugin = { version = "0.6.0", features = ["host"] }
libloading = "*"
libc = "*"
