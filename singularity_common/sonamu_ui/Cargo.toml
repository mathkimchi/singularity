[package]
name = "sonamu_ui"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
sonamu_sync = { workspace = true }

serde = { workspace = true }
serde_json = { workspace = true }

image = { workspace = true }

# just for winit backend
glyphon = "^0.11"
wgpu = "^29.0.3"
winit = "^0.30.13"
pollster = "^0.4.0"
bytemuck = { version = "^1.25.0", features = ["derive"] }
wgpu_canvas = { version = "^5.0.8" }
ttf-parser = "0.18" # is what msdf uses
msdfgen = "^0.2.1"
dejavu = "^2.37.0"

log = { workspace = true }

[features]
default = ["winit_backend"]
winit_backend = []
